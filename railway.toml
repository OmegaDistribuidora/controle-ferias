[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run prisma:generate --workspace backend && npm run build --workspace frontend"

[deploy]
startCommand = "npm run prisma:push --workspace backend && npm run start --workspace backend"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

